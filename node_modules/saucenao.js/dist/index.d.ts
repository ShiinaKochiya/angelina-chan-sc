import { RequestParams, Response } from './typing';
export declare class SauceNao {
    private url;
    constructor(dynamicParams?: RequestParams);
    find(staticParams: RequestParams | string): Promise<Response>;
}
