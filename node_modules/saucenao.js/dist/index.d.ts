import { FindResult, RequestParams } from './typing';
export declare class SauceNao {
    private url;
    constructor(dynamicParams?: RequestParams);
    find(staticParams: RequestParams | string): Promise<FindResult>;
}
